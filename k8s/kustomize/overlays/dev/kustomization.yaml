apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: gospeed-dev

resources:
  - ../../base

patches:
  - target:
      kind: Deployment
      name: gospeed-server
    patch: |
      - op: replace
        path: /spec/replicas
        value: 1
